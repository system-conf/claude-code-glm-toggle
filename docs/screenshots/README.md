# Screenshots

Drop screenshots here for the README. Suggested shots:

- `preview.png` — the activity bar panel open, showing both mode switches
- `status-bar.png` — the bottom-right indicator in GLM mode (yellow) and Claude mode (default)
- `command-palette.png` — `Ctrl+Shift+P` showing the `GLM Toggle:` commands

Recommended size: 1200–1600 px wide, PNG.
